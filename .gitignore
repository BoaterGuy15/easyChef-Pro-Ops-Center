# clasp OAuth credentials — contains access tokens, never commit
.clasprc.json

# Claude Code local settings — may contain OAuth client credentials
firebase-deploy/.claude/settings.local.json
.claude/settings.local.json

# Firebase cache files
firebase-deploy/.firebase/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# QA / audit test output
qa_*.json
post_check.json
